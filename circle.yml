dependencies:
  cache_directories:
    - ~/.meteor
  override:
    - meteor || curl https://install.meteor.com | /bin/sh

test:
  override:
    - cd app && meteor build ../output --architecture os.linux.x86_64

deployment:
  production:
    branch: "master"
    commands:
      - npm install -g pm2-meteor
      - cd deploy
      - scp -P 26771 xcv58@band.xcv58.com:app.tar.gz ./
      - pm2-meteor deploy
